<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <!-- Log file location -->
        <Property name="LOG_DIR">./logs</Property>
        <Property name="LOG_FILE_NAME">app.log</Property>
    </Properties>

    <Appenders>
        <RollingFile name="RollingFileAppender"
                     fileName="${LOG_DIR}/${LOG_FILE_NAME}"
                     filePattern="${LOG_DIR}/%d{yyyy-MM-dd}/${LOG_FILE_NAME}.%d{yyyy-MM-dd}-%i.gz">
            <!-- Daily rollover + size-based trigger -->
            <Policies>
                <!-- Time-based: rollover every day at midnight -->
                <TimeBasedTriggeringPolicy />

                <!-- Size-based: rollover when file reaches 10 MB -->
                <SizeBasedTriggeringPolicy size="10 MB"/>
            </Policies>

            <!-- Delete files older than 10 days -->
            <DefaultRolloverStrategy max="999">
                <Delete basePath="${LOG_DIR}" maxDepth="2">
                    <IfFileName glob="*/${LOG_FILE_NAME}.*.gz" />
                    <IfLastModified age="10d" />
                </Delete>
            </DefaultRolloverStrategy>

            <PatternLayout>
                <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</Pattern>
            </PatternLayout>
        </RollingFile>
    </Appenders>

    <Loggers>
        <Root level="info">
            <AppenderRef ref="RollingFileAppender"/>
        </Root>
    </Loggers>
</Configuration>
